import { ReactNode } from "react";

export interface ChatProfileUserProps {
    children?: ReactNode;
    // username: string;
    // userImage?: string;
    // userEmail: string;
    // userLocation?: string;
}